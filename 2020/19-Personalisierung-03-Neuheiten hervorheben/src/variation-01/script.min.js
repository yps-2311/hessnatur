/**
 * WATO.elem 			 ==> Polling auf ein Element
 * WATO.initObserver 	 ==> Initialisierung des MutationObservers
 * WATO.observer 		 ==> DOM Element dem Observer hinzufügen
 * WATO.qs 				 ==> Kurzform für document.querySelector
 * WATO.qsa 			 ==> Kurzform für document.querySelectorAll
 * WATO.ready 			 ==> Wartet auf document.ready
 * WATO.classlist 		 ==> Fügt mehrere Klassen einem Element hinzu oder entfernt sie
 * WATO.ajax 			 ==> Überprüfung auf Ajax-Aktivitäten auf der Seite
 * WATO.getCookie 		 ==> Ließt einen Cookie aus
 * WATO.setCookie 	     ==> Fügt einen Cookie hinzu
 * WATO.removeCookie 	 ==> Entfernt einen Cookie
 * WATO.exclude 		 ==> Punchout-Funktion
 * WATO.storageAvailable ==> Überprüft ob der LocalStorage verfügbar ist
 * WATO.reload           ==> Führt einen Reload aus (löscht dabei den #hash, als Workaround für manche Browser)
 * WATO.offsetY          ==> Gibt die Y-Position eines Elements zurück
 * WATO.scrollTo         ==> Scrollt bis zum Übergebenen Pixel (von oben)
 * WATO.event            ==> EventListener setzen
 * WATO.xhr_get          ==> Ajax GET Request
 * WATO.xhr_post          ==> Ajax POST Request
 */
!function(e,o){"use strict";void 0===e.WATO&&(
/**
         * @class
         * @namespace WATO
         * @constructor
         *
         * @author Max Vith
         * @see {@link http://usejsdoc.org/tags-param.html|JSDoc}, {@link http://www.w3schools.com/js/js_strict.asp|JavaScript Use Strict}
         */
e.WATO=function(){})
/**
     * @function elem
     * @memberOf WATO
     *
     * @author Max Vith
     * @description Polling with JavaScript and jQuery.
     * @see Inspiriert von {@link http://clearhead.me/a-new-monetate-polling-pattern-emerges/|Clearhead.me}.
     *
     * @param {string} selector - CSS Selector des zu findenden DOM Elements.
     * @param {function} callback - Auszuführende Funktion nachdem das Element gefunden wurde.
     * @param {number} [timeout=20] - Wiederholungsrate in Millisekunden.
     * @param {class} [self=WATO] - Web Arts Testing Object.
     *
     * @return {object} NodeList object - Returns all elements in the document that matches a specified CSS selector(s), as a static NodeList object.
     */,e.WATO.prototype.elem=function(e,t,s,n,r){var i=this||n,d=r||Date.now(),c=!1,a;return Date.now()-d>1e4?(t(!1),!1):!0===(c="string"==typeof e?(a=o.querySelectorAll(e)).length>0:!!(a=e()||!1))?t(a):setTimeout(i.elem.bind(null,e,t,s,i,d),s||20)},
/**
     * @function qs
     * @memberOf WATO
     *
     * @author Timo Ott
     *
     * @param {string} selector - CSS Selector des zu findenden DOM Elements
     */
e.WATO.prototype.qs=function(e,t){return(t||o).querySelector(e)},
/**
     * @function qsa
     * @memberOf WATO
     *
     * @author Timo Ott
     *
     * @param {string} selector - CSS Selector des zu findenden DOM Elements
     */
e.WATO.prototype.qsa=function(e,t){return(t||o).querySelectorAll(e)},
/**
     * @function ready
     * @memberOf WATO
     *
     * @author Max Vith
     *
     * @param {function} callback
     */
e.WATO.prototype.ready=function(e){
// http://youmightnotneedjquery.com/ IE9+
(o.attachEvent?"complete"===o.readyState:"loading"!==o.readyState)?e():o.addEventListener("DOMContentLoaded",e);
// BUGFIX LD 09.05.2017
// var state = document.readyState !== 'loading';
// if(state) {
// 	callback();
// } else {
// 	document.addEventListener('DOMContentLoaded', callback);
// }
// return state;
},
/**
     * @function initObserver
     * @memberOf WATO
     *
     * @author Max Vith
     * @description Erstellt einen Mutation Obeserver auf den gesamten DOM. 
     *
     * @param {function} errorCallback - Callback-Funktion bei einem Fehler des Mutation Observers
     */
/*
    window.WATO.prototype.initObserver = function(errorCallback){

        var _self = this;

        try {
            
            if(!window.WATOObserver){

                // Testübergreifendes Array für den Mutation Observer
                window.WATOObserver = [];

                // create an observer instance
                var observer = new MutationObserver(function(mutations){

                    // console.log(mutations);
                    mutations.forEach(function(mutation) {

                        mutation.addedNodes.forEach(function(node){

                            if(node.nodeType === 1){

                                window.WATOObserver.forEach(function(elem, index){

                                    if(node.matches(elem[0])){

                                        // run callback function
                                        elem[1](_self.qsa(elem[0]));

                                        // remove entry
                                        window.WATOObserver.splice(index, 1);

                                        // break the loop
                                        return false;
                                    }
                                });
                            }
                        });
                    });
                });
                
                observer.observe(document, { 
                    childList: true, 
                    subtree: true,
                    // attributes: true,
                    // characterData: false 
                });

                _self.ready(function(){

                    observer.disconnect();

                    window.WATO.prototype.observer = function(selector, callback){

                        callback(_self.qsa(selector));
                    };
                });
            }
        } catch(error) {

            if(typeof errorCallback === "function"){

                errorCallback(error);
            }
        }
    };
    */
/**
     * @function observer
     * @memberOf WATO
     *
     * @author Max Vith
     *
     * @param {string} selector - CSS Selector des zu findenden DOM Elements
     * @param {function} callback - Callback-Funktion für gefundenene DOM ELemente
     */
/*
    window.WATO.prototype.observer = function(selector, callback){

        window.WATOObserver.push([selector, callback]);
    };
    */
/**
     * @function classlist
     * @memberOf WATO
     *
     * @author Pascal Martin
     *
     * @description Benutzung z.B.: WATO.classlist("h1", ["Eins", "Zwei", "Drei"], true); oder WATO.classlist("h1", ["Eins", "Zwei", "Drei"]); oder WATO.classlist("h1", ["Eins", "Zwei", "Drei"], false);
     *
     * @param {string} selector - CSS Selector des DOM Elements was geändert werden soll
     * @param {string} action - Erwartet "add" zum hinzufügen oder "remove" zum löschen von Klassen
     * @param {array} classes - Erwartet ein Array mit allen Klassen die hinzugefügt oder entfernt werden sollen
     */
/*
	window.WATO.prototype.classlist = function (selector, classes, action) {
		var el = document.querySelector(selector).classList;
		for (var z = 0; z < classes.length; z++) {
			if(action || action === undefined){
				el.add(classes[z]);
			}else{
				el.remove(classes[z]);
			}
		}
	};
	*/
/**
     * @function ajax
     * @memberOf WATO
     *
     * @author Lukas Dziambor & Max Vith
     *
     * @param {string} url -
     * @param {function} callback -
     */
/*
    window.WATO.prototype.ajax = function(url, callback) {

        var request = XMLHttpRequest.prototype.open;

        XMLHttpRequest.prototype.open = function(method, uri, async, user, pass) {

            this.addEventListener("loadend", function() {

                if (this.readyState === 4) {

                    if (uri.indexOf(url) !== -1) {
                        
                        callback();
                    }
                }
            }, false);

            request.call(this, method, uri, async, user, pass);
        };
    };
    */
/**
     * @function getCookie
     * @memberOf WATO
     *
     * @author Max Vith
     *
     * @param {string} name - Cookie name
     */
/*
	window.WATO.prototype.getCookie = function(name){
		
		var cookies = document.cookie.split(";");
		
		for(var i = 0; i < cookies.length; i++){
			
			if(cookies[i].substr(0, cookies[i].indexOf("=")).replace(/^\s+|\s+$/g,"") === name){
				
				return decodeURIComponent(cookies[i].substr(cookies[i].indexOf("=") + 1));
			}
		}
		
		return false;
	};
	*/
/**
     * @function setCookie
     * @memberOf WATO
     *
     * @author Max Vith
     *
     * @param {string} name - Cookie name
     * @param {string} value - Cookie value
     * @param {string} domain - Domain like .exaple.com
     * @param {Boolean} session - true = session cookie, false = permanent cookie
     */
/*
	window.WATO.prototype.setCookie = function(name, value, domain, session){
		
		var exdate = new Date();
		
		exdate.setDate(exdate.getDate() + 365);
		
		document.cookie = name + "=" + encodeURIComponent(value) + ";" + (!session ? "expires=" + exdate.toUTCString() + ";" : "") + "domain=" + domain + ";path=/";
	};
	*/
/**
     * @function removeCookie
     * @memberOf WATO
     *
     * @author Max Vith
     *
     * @param {string} name - Cookie name
     * @param {string} domain - Domain like .exaple.com
     */
/*
	window.WATO.prototype.removeCookie = function(name, domain){
		
		document.cookie = name + "=false;expires=Thu, 01 Jan 1970 00:00:01 GMT;domain=" + domain + ";path=/";
	};
	*/
/**
     * @function exclude
     * @memberOf WATO
     *
     * @author Max Vith
     *
     * @param {number} width -
     * @param {function} callback
     */
/*
	window.WATO.prototype.exclude = function(width, callback){
		
		function _checkWidth() {
			
			var current = window.innerWidth || document.body.clientWidth;
			
			if(current <= width && !exclude){
				
				exclude = true;
				
				callback();
			}
		}
		
		var exclude = false;
		
		_checkWidth();
		
		if(typeof callback === "function"){
			
			window.onresize = function() {

				_checkWidth();
			};
		}
	};
	*/
/**
     * @function storageAvailable
     * @memberOf WATO
     *
     * @author Max Vith
     *
     * @returns {Boolean} - true = private / inkognito
     */
/*
	window.WATO.prototype.storageAvailable = function(){
		
		var mod = 'wato';
	    
	    try {
	    	
	        window.localStorage.setItem(mod, mod);
	        window.localStorage.removeItem(mod);
	        
	        return true;
	    } catch(e) {
	
	        return false;
	    }
	};
    */
/**
     * @function reload
     * @memberOf WATO
     *
     * @author Lukas Dziambor
     */
/*
	window.WATO.prototype.reload = function(){
        location.reload();
        location.href=location.href.split('#')[0];
	};
    */
/**
     * @function offsetY
     * @memberOf WATO
     *
     * @author Lukas Dziambor
     * 
     * @returns {integer} - y-position from top in pixel
     */
/*
	window.WATO.prototype.offsetY = function(el){
        var curtop = 0;
        if (el.offsetParent) {
            while (el.offsetParent) {
                curtop += el.offsetTop;
                el = el.offsetParent;
            }
        }
        else if (el.y){
            curtop += el.y;
        }
          
        return curtop;
	};
    */
/**
     * @function scrollTo
     * @memberOf WATO
     *
     * @author Lukas Dziambor
     */
/*
	window.WATO.prototype.scrollTo = function(pixel, duration){
        Math.easeInOutQuad = function (t, b, c, d) {
            t /= d/2;
            if (t < 1){
                return c/2*t*t + b;
            }else{
                t--;
                return -c/2 * (t*(t-2) - 1) + b;
            }
        };

        var start = window.document.documentElement.scrollTop + window.document.body.scrollTop,
        change = pixel - start,
        currentTime = 0,
        increment = 20,
            
        animateScroll = function(){        
            currentTime += increment;
            var val = Math.easeInOutQuad(currentTime, start, change, duration);

            window.document.documentElement.scrollTop = val;
            window.document.body.scrollTop = val;

            if(currentTime < duration) {
                setTimeout(animateScroll, increment);
            }
        };
        animateScroll();
	};
    */
/**
     * @function event
     * @memberOf WATO
     *
     * @param {node} element - DOM Node die den Listener erhalten soll
     * @param {string} - Eventtype z.B. click
     * @param {function} callback - Auszuführende Funktion bei Trigger.
     * 
     * @author Lukas Dziambor
     */
/*
	window.WATO.prototype.event = function(element, type, callback){
        element.addEventListener(type, callback);
	};
    */
/**
     * @function xhr_get
     * @memberOf WATO
     *
     * @param {string} url - URL die angefragt werden soll
     * @param {function} callback - Auszuführende Funktion nachdem der Request erfolgreich beendet wurde.
     * @param {object} scopedData - Daten die im Scope des Callbacks verfügbar sein sollen
     * 
     * @author Lukas Dziambor
     */
e.WATO.prototype.xhr_get=function(e,o){var t=new XMLHttpRequest;t.open("GET",e,!0),t.onload=function(){if(4===this.readyState&&this.status>=200&&this.status<400)try{
// var data = JSON.parse(this.response);
o(this.response)}catch(e){o(!1)}else
// We reached our target server, but it returned an error
o(!1)},t.onerror=function(){
// There was a connection error of some sort
o(!1)},
// request.withCredentials = true;
t.send()}}(window,document),
/*
 * !LOAD WATO OBJECT
 * @codekit-prepend "../vendor/WATO.js";
 * 
 * OPTIONAL
 * @ codekit-append "pds.js"; 
 */
/**
* @function
* @author Manuel Brueckmann
* @namespace
* @name Global Script for lookup customer type
* @description Prototype for requesting Econda ARP API for user profiles
*/
function(e,o){"use strict";
// init the campaign overall
function t(){
// eg. trigger widget
s(void 0!==sessionStorage.getItem("kk_targetGroup")?sessionStorage.getItem("kk_targetGroup"):"interessent"),
// eg. trigger content, visual changes depending on design
n(void 0!==sessionStorage.getItem("kk_targetGroup")?sessionStorage.getItem("kk_targetGroup"):"interessent")}
// placeholder for econda widget call and rendering to specific container
function s(e){console.log("triggered econda widget: "+e)}
// placeholder for campaign stuff
function n(e){console.log("triggered campagin visuals and content for: "+e)}
// get user classification, only once per session
// already in session storage?
o.sessionStorage.getItem("kk_targetGroup")?t():
// 1. check if user is logged in > it's "neukunde" or "bestandskunde" then
// TODO: Polling or DOM-Ready to check if form-wrapper is present
null===document.getElementById("_loginComponentForm")?// form not present, so user is logged in
// check if more than 1 order (ajax request to the my-account ajax content)
e.xhr_get("https://www.hessnatur.com/de/my-account/orders",(function(e){
// amount of orders
console.log("orders total: ",e.split("js_orderHistoryItem-").length),e&&(
// neukunde, only 0 or 1 order
// TODO: what if js_orderHistoryItem is not available? Error then?
e.split("js_orderHistoryItem-").length<2?sessionStorage.setItem("kk_targetGroup","neukunde"):sessionStorage.setItem("kk_targetGroup","bestandskunde")),
// init campaign (regardless if request was succesfull or not, fallback to 'interessent' then, next attempt will try again)
t()})):
// 2. if not, check if econda does know something about visitor
// visitor is not logged in (which will be mainly the case)
e.elem((function(){// econda customer id (only available if logged in and econda is not adblocked)
// check if econdas emos3 is available
// due to this might take some time (async), wait for it
return console.log("window.emos3: ",o.emos3),// econda object
console.log("window.emos3.emos_vid: ",o.emos3.emos_vid),// econda visitor id (shoud be always available, default)
console.log("window.emos3.emos_cid: ",o.emos3.emos_cid),void 0!==o.emos3&&(void 0!==o.emos3.emos_vid||void 0!==o.emos3.emos_cid)}),(function(s){s&&(
// econda is available
// ARP endpoint:	https://services.crosssell.info/profileaccess/00002762-7fbb585b-0c52-33a0-ad30-b2319526ea2f/profiles/cgroup?emcid|emvid=
// Example queries:
// Neukunden:		https://services.crosssell.info/profileaccess/00002762-7fbb585b-0c52-33a0-ad30-b2319526ea2f/profiles/cgroup?emcid=de2c709c269261dbe7cf230465a205b1
//					https://services.crosssell.info/profileaccess/00002762-7fbb585b-0c52-33a0-ad30-b2319526ea2f/profiles/cgroup?emvid=AXG7X26WccAN3yiAKoMG9Xw*UWd9pDAw
// Bestandskunden:	https://services.crosssell.info/profileaccess/00002762-7fbb585b-0c52-33a0-ad30-b2319526ea2f/profiles/cgroup?emcid=c52d407f90b141afb905d6da8f228495			
// Interessent: 	https://services.crosssell.info/profileaccess/00002762-7fbb585b-0c52-33a0-ad30-b2319526ea2f/profiles/cgroup?emvid=AXTjATsJh0ydB8cRFsQD8PFCiJUTldmy
// build arp endpoint url by available profile id and request data
console.log("https://services.crosssell.info/profileaccess/00002762-7fbb585b-0c52-33a0-ad30-b2319526ea2f/profiles/cgroup?"+(void 0!==o.emos3.emos_cid?"emcid="+o.emos3.emos_cid:"emvid="+o.emos3.emos_vid)),e.xhr_get("https://services.crosssell.info/profileaccess/00002762-7fbb585b-0c52-33a0-ad30-b2319526ea2f/profiles/cgroup?"+(void 0!==o.emos3.emos_cid?"emcid="+o.emos3.emos_cid:"emvid="+o.emos3.emos_vid),(function(e){
//WATO.xhr_get('https://services.crosssell.info/profileaccess/00002762-7fbb585b-0c52-33a0-ad30-b2319526ea2f/profiles/cgroup?emcid=c52d407f90b141afb905d6da8f228495', function (response) {
console.log("response: ",e),e&&(-1!==e.indexOf("Bestandskunde")?
// bestandskunde (highest priority, even if Neukunde is also present in response)
sessionStorage.setItem("kk_targetGroup","bestandskunde"):-1!==e.indexOf("Neukunde")?
// neukunde
sessionStorage.setItem("kk_targetGroup","neukunde"):sessionStorage.setItem("kk_targetGroup","interessent")),
// init campaign (regardless if result is available or not)
t()})))}))}(new window.WATO,window),new window.WATO;